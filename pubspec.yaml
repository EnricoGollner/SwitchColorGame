name: color_switch_game
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.21.0
  flame_audio: ^2.11.6
  mocktail: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flame_test: ^1.19.2
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/audio/
